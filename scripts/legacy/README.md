# Historical evaluation-manifest utility

`build_evaluation_manifests.py` reflects an earlier M1/M4/M2 split workflow. It
does not create the final chromosome-03-and-05 external evaluation used in the
manuscript and is retained only for provenance. Use the coordinate manifests in
`manifests/` and the current frozen-probe and LoRA scripts for reported results.
